apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc
  namespace: nginx-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi          # Request size